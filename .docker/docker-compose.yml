services:
  npm:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ..:/app
    ports:
      - "8080:8080"
    entrypoint: ["npm"]

  dev:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ..:/app
    ports:
      - "8080:8080"
    command: npm run dev
